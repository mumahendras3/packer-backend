{
  "name": "packer-server",
  "version": "0.0.1",
  "description": "This is the server side of Packer, a client-server software suite for helping a package maintainer keep track of the latest version of the software they are packaging and also perform some automated tasks like building and testing the package in a docker container for a clean and reproducible environment.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx nodemon index.js"
  },
  "author": "mumahendras3",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.3.4",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongoose": "^7.0.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
